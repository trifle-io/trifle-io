{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "trifle-io.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "trifle-io.labels" . | nindent 4 }}
{{- end }}
